# include <stdio.h>
int main(){
    int r,c;
    printf("enter the no of rows :");
    scanf("%d",&r);
    printf("enter the no. of the columns :");
    scanf("%d",&c);
    int matrix[r][c];
    for(int)
    return 0;
}